'  new idea perhaps this works better ?
'
Sub exact_fallback()
               
    DIM exact AS Integer
    exact = -16.0
    If exact = -16.0 Then
       Print exact*2
    EndIf 
            
End Sub            