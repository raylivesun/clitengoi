/'
The name of the package will be automatically generated when the package
squares the package name with the package name of the package
'/
#lang "fblite"
Declare Function PortNameDefaultTypes (ByVal Counter As Any Ptr, ByVal CounterNumber As Const UByte Ptr, ByVal ObjValueCategory As ULong)  

'' list of edge types 
     Dim CounterList As String 
     Dim CounterName As String
     Dim CounterType As String
     Dim CounterValue As String
     Dim CounterDescription As String
     Dim CounterCategory As String
     Dim CounterUnit As String
     Dim CounterMin As String
     Dim CounterMax As String
     Dim CounterStep As String
   
End